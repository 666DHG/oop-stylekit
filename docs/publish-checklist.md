# Publish Checklist

- Confirm the course style rules match the current homework requirements.
- Choose and add a license before publishing publicly.
- Commit the initial release files.
- Create a `v0.1.0` tag after the first public commit.
- Add a short repository description such as `C++ formatter and linter config for OOP homework`.
